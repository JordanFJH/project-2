

function SageTransform({ word, setSagePhrase }) {

    // console.log(word);
    console.log("In the transform")

    return (
        <div>
            This is the page to handle all Sage Translation
        </div>
    );
}

export default SageTransform;