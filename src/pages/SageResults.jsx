
function SageResults({ setShowResult }) {
    return (
        <div>
            <button onClick={() => setShowResult(false)}>Return to Smartical</button>
            <h1>This is the page for the Sage results</h1>
        </div>
    );
}

export default SageResults;